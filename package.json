{
  "name": "screw-marble-drop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate.mjs",
    "inspect": "node scripts/inspect.mjs",
    "joints": "node scripts/joints.mjs",
    "validate:all": "node scripts/validate.mjs && node scripts/validate.mjs --level 2"
  },
  "dependencies": {
    "@types/three": "^0.185.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.6"
  }
}
